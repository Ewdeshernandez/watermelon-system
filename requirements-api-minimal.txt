# Dependencias mínimas para correr SOLO el servicio HTTP api/.
# Versión liviana sin streamlit/plotly/kaleido/reportlab para que el
# deploy en Render free tier sea rápido (cold start < 30s).
#
# Para correr el API standalone:
#   pip install -r requirements-api-minimal.txt
#   uvicorn api.app:app --host 0.0.0.0 --port 8000

fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
numpy>=1.24.0
pandas>=2.0.0
# Ciclo 19: la API necesita Supabase para acceder al archivo de reportes
# (mismo bucket que usa la app Streamlit).
supabase>=2.0.0
