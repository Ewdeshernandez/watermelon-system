numpy
pandas
streamlit
scipy
# matplotlib es requerido por core/sensor_diagram.py (turbomachinery
# silhouette + polar per plane) y por core/plot_export.py.
# Antes venia como dep transitiva pero un rebuild de Streamlit Cloud
# en v2.5 dejo de incluirlo. Agregado explicitamente para que main
# nunca pierda el diagrama del Sensor Map.
matplotlib>=3.7.0
plotly==6.1.1
kaleido==1.0.0
reportlab>=4.0.0
# Ciclo 9: persistencia real del Vault contra Supabase (Postgres + Storage).
# Si st.secrets[supabase] no está configurado, la app cae a filesystem
# local automáticamente — supabase queda como dependencia opcional.
supabase>=2.0.0
# Ciclo 15.2: click-to-place de sensores sobre el schematic_png del activo
# para que el Resumen Ejecutivo y Machine Map rendericen los valores
# Overall + colores de severidad sobre la foto/dibujo real, no el
# turbomachinery genérico.
streamlit-image-coordinates>=0.1.7
# Pillow para overlay de markers + texto sobre el schematic_png
Pillow>=10.0.0
# Ciclo 17.26: AI Diagnóstico con Claude API. Generación de
# interpretación clínica + recomendaciones en español natural a
# partir de los hallazgos técnicos de cada análisis (Spectrum,
# Waveforms, Trends, Bode, Polar, Orbit). Si st.secrets[anthropic]
# no está configurado, el botón "🧠 Diagnóstico AI" aparece deshabilitado
# pero el resto del sistema funciona normal.
anthropic>=0.39.0
# Ciclo 23.66: PNG export removido por completo. Probamos cairosvg (libcairo
# system lib falta), svglib (rlpycairo → pycairo → libcairo), y resvg-py
# (wheel se instala pero falla en runtime con nuestro SVG). El SVG vectorial
# cubre el 95% del caso de uso real (PowerPoint, Keynote, Word, email todos
# lo soportan nativamente). Si más adelante necesitamos PNG inline, el camino
# correcto es render client-side via canvas (browser hace el trabajo).
# rebuild trigger
