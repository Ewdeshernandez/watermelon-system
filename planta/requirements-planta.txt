# Watermelon Planta Edition · Dependencias mínimas
# ===================================================
# Solo lo necesario para CAPTURAR en planta sin internet.
# Sin Supabase, sin Anthropic, sin imageio-ffmpeg, sin reportlab, sin kaleido.
# Total: ~80 MB vs ~500 MB del Watermelon completo.
#
# Instalación offline desde wheels/:
#   pip install --no-index --find-links wheels/ -r requirements-planta.txt
#
# Instalación online (con internet):
#   pip install -r requirements-planta.txt

# Captura de adquisición (requiere driver del fabricante instalado)
nidaqmx>=1.0.0

# Lectura/escritura formato TDMS
npTDMS>=1.7.0

# Streamlit para la UI
streamlit>=1.40.0

# Cómputo numérico
numpy>=1.24.0
pandas>=2.0.0

# Plotting (para mostrar previews de la captura en la UI)
plotly>=5.18.0

# HTTP para futuros añadidos
requests>=2.30.0

# Supabase para sync uploader (FASE B v3.31.209)
# Solo se necesita si vas a sincronizar TDMS al Watermelon Cloud.
# Funciona sin esto si solo capturas offline.
supabase>=2.0.0

# Verificación de licencias RSA-signed (FASE D v3.31.215)
# OBLIGATORIO — la app no arranca sin license_manager.py funcional.
pyjwt>=2.8.0
cryptography>=41.0.0

# Tray icon para Windows (FASE L v3.31.233)
# El launcher.py corre Streamlit en background y muestra un ícono en la
# bandeja del sistema con menú Abrir/Salir. Esto elimina la ventana
# negra cmd.exe que se veía antes.
pystray>=0.19.5
Pillow>=10.0.0  # ya viene con cairosvg/streamlit pero lo pinneamos
